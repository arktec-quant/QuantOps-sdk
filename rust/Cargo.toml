[package]
name = "quantops-sdk"
version = "0.1.1"
edition = "2021"
license = "MIT"
description = "Runtime-neutral validation for quantops-evidence-bundle/v1"
repository = "https://github.com/arktec-quant/QuantOps-sdk"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
