# Changelog

## v0.1.1

- Aligned the Rust crate's package name with the Python distribution:
  `quantops-sdk`.

## v0.1.0

- Introduced `quantops-evidence-bundle/v1` for deterministic, artifact-first
  display evidence.
- Added matching Python and Rust validators with shared conformance fixtures.
- Added safe publication helpers for metadata-only allowlisted artifacts.
- Established a public MIT-licensed source release.
